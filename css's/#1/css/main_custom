/*

A css by L-U-C-A

V 0.03

Last Changes:
    - added last changes section
    - moved spectate btn
    - fixed customize button

*/

.verticalSeparator, #editorBtnM, .menuSocialB, #faceItIconM, #rankedSoonTm, #menuClassName, #classPreviewCanvas, #menuClassSubtext, #bubbleContainer, #uiBase.onMenu #voiceDisplay, #tlInfHold, #streamContainer, #newsHolder, #endAContainer, #endAMerger, .endAHolder, #aContainer, #aMerger, #aHolder {
    display: none !important;
    opacity: 0 !important;
}
#menuItemContainer {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100px;
    top: calc(100% - 100px);
}

#uiBase.onMenu #chatHolder {
    position: absolute;
    bottom: 120px;
    left: 20px;
}

#mapInfoHolder {
    position: absolute;
    bottom: 120px;
}

#signedOutHeaderBar {
    top: 75px;
    height: 90px;
    background: none;
    left: 16px;
}

#menuClassContainer {
    align-items: flex-start;
    width: 343px;
    left: 50px;
    top: 0;
    height: 90px;
}

.headerBarRight {
    left: 70%;
    top: calc(100% - 120px - 80px / 2 - 20px);
    background-color: transparent;
}

#subLogoButtons {
    display: inline-flex;
    flex-direction: column-reverse;
    left: calc(100% - 95px);
    top: calc(50% - 500px / 2);
    height: 500px;
    justify-content: center;
}

#uiBase.onMenu #spectButton {
    left: calc(19.5% + 20px);
    top: calc(100% - 120px - 18px / 2 - 20px);
}

#customizeButton {
    width: 263px;
}
